import { FilterByTrademarkPipe } from './filter-by-trademark.pipe';

describe('FilterByTrademarkPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTrademarkPipe();
    expect(pipe).toBeTruthy();
  });
});
